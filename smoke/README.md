# Smoke Gates — Status Board

Feasibility smoke gates per `docs/EXPERIMENT_ARCHITECTURE_FINAL.md` Part G. **No v0.5, no
sealing, no confirmatory corpus until every in-scope gate passes on the pilot corpus.** Each gate
is a minimal runnable spike, not an experiment. A failing gate stops its branch and applies the
gate-outcome policy (fallbacks), recorded as an ADR.

**Execution DAG (respect dependencies):**

```
G-1 / G-5 / G-8  →  G-6 / G-7  →  G-2 / G-4 / G-11  →  G-12 / G-13  →  G-9 / G-14  →  G-10
```

G-3 (latency spike) may run any time after G-1, but its threshold is fixed beforehand
(`docs/frozen_parameters.md` item 2, currently UNSET). Claim-dependent gates (G-3, G-14, G-15,
INV-only check) run only for claims retained in the sealed scope.

Run a gate: `make gate GATE=g1`

**Apparatus note, 2026-07-30 (no gate status changed by it).** The experimental apparatus the
waiting gates need now exists: the golden thread runs end to end — Supervisor → A2A delegation
hop (behind a port; `a2a-python` still unpinned, ADR 0020) → Specialist → MCP tool call over the
frozen `Ω` — under **`B0`** (no delegation protection) and **`B3`** (the full control layer, its
ten §A.5 conjuncts each a named function with its own reason code), with the pilot corpus
(`fixtures/pilot/golden_thread/`), the harness runner, the SUT-side capability signer (D21's
independent implementation, agreement-tested against the harness verifier), and Phase-1 OAuth
provisioning (ADR 0021). **This pass adjudicated no gate, edited no row below, and set no
evidence grade**; every "not started" is still not started, and `IA-3` remains
`[UNVERIFIED-IA]` — the RQ4 timing seams exist but **nothing was measured** (rows 1–2 UNSET).

**Apparatus note, 2026-07-31.** *(The note above was true when written; this supersedes its arm
list and nothing else.)* Two more arms exist — **`B2-exchange-task`** (the fair strong OAuth arm:
online RFC 8693 exchange per hop, narrowed to `C_i` under the ADR 0017 RAR type) and **`B-cap`**
(offline attenuation, bearer capability, `oauth_authn = 1`) — over a **four-scenario** pilot
corpus, the fourth being `gt-f1-chain-tamper`. `frozen_parameters` rows 4/6/10 are frozen as
`H(Λ)` (ADR 0022, composition amended by ADR 0023) and the delegating client's Phase-1 grant is
fixed by ADR 0024. **G-13 is adjudicated by this pass and its row below is updated; no other row
moved**, and `IA-3` is untouched — G-13 establishes matched authority, not cost, and **no timing
number was produced anywhere**.

| Gate | Depends on | Status | Report | ADR |
|------|-----------|--------|--------|-----|
| G-1 | — | **PASS** — restored after the ADR 0003 corrective suite passed (10/10 regression tests): commitment is now the versioned BlockID scheme (encoding-independent, fail-closed); biscuit-python==0.4.0 pinned; G-1.G′ replaced the seal check (ADR 0002) | [g1/REPORT.md](g1/REPORT.md) | [0002](../adr/0002-python-biscuit-library.md), [0003](../adr/0003-capability-commitment-scheme.md) |
| G-2 | G-6/G-7 | **PASS** — the first authorizer-with-policies run in the project, against the `Ω`/`Γ` frozen by ADR 0016 (`H(Γ) = f63320c9da3731a6ea04dc51d9f6852f3a3e130182ce3a7fe251158751333deb`), with every `C_i` **computed** by 7 authorizer runs per prefix, never asserted: (a) a widening append verifies under `κ_pub` yet leaves `C_i ⊆ C_{i−1}` — extended to six broadening vectors incl. rules, `expiry` and `token_audience`, two probed under their unlocking condition — while legitimate narrowing gives `C_2 ⊊ C_1 ⊊ C_0 = U_task`; (b) a **fully constructed** attacker-signed third-party block is rejected structurally pre-evaluation (the library itself verifies it under `κ_pub`, so the project check is load-bearing) and carries no authority if evaluated anyway, and a `trusting {attacker_key}` authorizer is refused pre-evaluation though it would otherwise admit; (c) five trust-broadening/`Ω`-widening mutations each change `H(Γ)` while a re-serialization does not, artifact byte-intact; (d) `−attenuation` admits exactly `C_0 \ C_n` more, differing in exactly `evaluation.prefix`. **IA-2 verified by this gate**; residuals: exact pin, scoped to these frozen bytes, SUT-side independent implementation still owed (D13/D21, G-13) | [g2/REPORT.md](g2/REPORT.md) | [0002](../adr/0002-python-biscuit-library.md), [0003](../adr/0003-capability-commitment-scheme.md), [0016](../adr/0016-omega-gamma-freeze.md) |
| G-3 | G-1 (threshold fixed beforehand) | not started | — | — |
| G-4 | G-6/G-7 | **PASS — now a full four-limb closure, completed by G-11.** It first passed over its *adjudicable* limbs only. Phase 1 read RFCs 8693/9396/8707/9449/9068 + OAuth 2.1 from the text and found `authlib==1.7.2` **UNSUPPORTED** ([g4/probe_authlib.py](g4/probe_authlib.py)), so **IA-4 is discharged by its second limb**: a behaviourally faithful AS **was built** at `src/sut/oauth_as/` (ADR 0015/0017) on the stdlib + `joserfc`, with **no new dependency pinned** and `authlib` still unpinned. Phase 2 adjudicated L1 (a task-narrowed token issues, granted RAR echoed, scope reported when it differs), **L1′** (widening refused with the exact error and **no token issued** in all four planes — asserted on the *absence* of a token, since a silent clamp is the failure mode), **L2** over the **frozen `Ω`** with no stand-in (both planes enforced; inside-RAR-outside-scope and inside-scope-outside-RAR both denied; wrong-`aud` token rejected), L3 (`actor→holder` resolves, unmapped actor rejected, `resource_owner = holder` never required, nested `act` present but only the outermost consulted), and A1–A7 (delegation not impersonation; 24 catalogue checks with exact codes/statuses; the RFC 9396 §12 string rule incl. NFC-vs-NFD; key isolation with both ADR 0015 import rules asserted and a seedless forge attempt rejected; and the **first real exercise** of the G-5 `ath` and nonce hand-forwards plus `htu` normalization). **Both residual limbs are now CLOSED by G-11** (2026-07-29, after this row first read "not a full four-limb closure"): **L4** — `INV.access_token_hash` verified through the real HTC/INV verifier with a swapped token rejected, the construction fixed by ADR 0018; and **L3** — the `actor→holder` limb re-run against the **frozen** registry (ADR 0019) instead of the C3 stand-in, outcome unchanged, which confirms the stand-in had not flattered it. **Still open:** `may_act` is spike-local pending `frozen_parameters` row 5, so F2 `wrong_principal` is not yet scored; and the AT profile is RFC 9068-**shaped**, deliberately **not** conformant (§8.3) | [g4/REPORT.md](g4/REPORT.md) · [g4/DESIGN.md](g4/DESIGN.md) (design) · [g4/SCOPE.md](g4/SCOPE.md) | [0008](../adr/0008-g4-spike-parallelisation.md), [0015](../adr/0015-as-placement.md), [0016](../adr/0016-omega-gamma-freeze.md), [0017](../adr/0017-as-profile-phase2.md), [0018](../adr/0018-access-token-hash.md), [0019](../adr/0019-identity-plane-registry.md) |
| G-5 | — | **PASS** — `joserfc==1.7.4` pinned for the JOSE surface only (ADR 0006): cnf/jkt binding with the RFC 8037 A.3 known answer, RFC 9449 §4.3 subset verifier, wrong-holder rejected at the thumbprint comparison; AS simulated locally — the real AS stays gated on G-4 (authlib pending line unchanged) | [g5/REPORT.md](g5/REPORT.md) | [0006](../adr/0006-dpop-jose-library.md) |
| G-6 | G-1/G-5/G-8 | **PASS** — `mcp==1.28.1` pinned (ADR 0013): full dispatch-path enumeration with file:symbol citations; wrap-at-fn + wrap-on-insert interposition mediates every enumerated path (7 bypass attempts all mediated or blocked); denied calls never execute (witness-verified); exactly one event on the error path; non-vacuity confirmed with the interposition removed | [g6/REPORT.md](g6/REPORT.md) | [0013](../adr/0013-mcp-sdk-pin.md) |
| G-7 | G-1/G-5/G-8 | **PASS** — exclusive-share ledger process (Win32 `FILE_SHARE_READ`-only handle): all SUT write/amend/delete attempts fail at the OS level (chmod-proof), records survive a lying SUT self-report, correlation ids intact, zero entries for unreached tools; `ingress_request_digest` settled as `H_JCS` (ADR 0012) | [g7/REPORT.md](g7/REPORT.md) | [0012](../adr/0012-ingress-digest-construction.md), [0013](../adr/0013-mcp-sdk-pin.md) |
| G-8 | — | **PASS** — `rfc8785==0.1.4` pinned (ADR 0005): encoding-invariant, RFC-vector-conformant (Appendix B 24/24; `canonicaljson` demonstrated non-conformant and rejected), separate-process signer/verifier agreement, fail-closed; the frozen `H_JCS` construction (hash/tag/encoding) recorded as an open decision | [g8/REPORT.md](g8/REPORT.md) | [0005](../adr/0005-jcs-library.md) |
| G-9 | G-12/G-13 | not started | — | — |
| G-10 | all prior DAG gates | not started | — | — |
| G-11 | G-6/G-7 | **PASS** — the HTC/INV verifier built at `src/harness/verifier/` implements every §F.2 MUST as a separately named check with its own reason code, so a rejection is attributable. **All fourteen mutations rejected for the condition each targets:** the eight HTC families (wrong-signer, parent-swap, child-swap, depth-rollback, capability-swap, terminal-key-mismatch, domain-tag confusion, expired/`nbf`-violating cert) and the six commitment-layer ones re-tested through the verifier — the latter already `[VERIFIED]` at the commitment layer by the ADR 0003 suite, so this is a **re-test, not a first verification**. Both positive arms pass, **including the `n = 0` zero-hop case**, with no branch keyed on the chain length (behavioural + AST assertions). Two findings recorded rather than smoothed over: the identity-plane check was **masking `htc_chain_linkage`**, so a case was added to isolate it; and the row's blanket "each rejected" **cannot** apply to a *semantically equivalent* container re-encoding, which ADR 0003 requires to be **accepted** with the commitment unchanged — rejecting it would reintroduce the false-rejection bug ADR 0003 fixed (the Part G row was **not** edited). Also closes **`access_token_hash`** (ADR 0018 — distinct from `ath`, `H_JCS` and a bare digest) and **freezes the identity-plane registry** (ADR 0019, `H(R) = d1bfc5ff…`, `frozen_parameters` row 11), which together close G-4's residual limbs **L4** and **L3**. **Residuals:** `label_assertions_digest`/`authz_context_hash` stay category (c) for G-15; the SUT-side INV signer is owed (D21, G-13); the jti cache is G-9; row 5 stays UNSET so F2 `wrong_principal` stays unscored; **IA-3 stays [UNVERIFIED-IA] for G-3** — correctness, not cost | [g11/REPORT.md](g11/REPORT.md) | [0018](../adr/0018-access-token-hash.md), [0019](../adr/0019-identity-plane-registry.md), [0003](../adr/0003-capability-commitment-scheme.md) |
| G-12 | G-2/G-4/G-11 | not started | — | — |
| G-13 | G-2/G-4/G-11 | **PASS over the arms that exist, `B2-exchange-task-DPoP` and `B3⁺` limbs OPEN** — the independent verifier at `src/harness/verifier/matched_authority.py` recomputes each arm's per-hop authority from **raw presented evidence** (one membership decision per element of `Ω`, the G-2 discipline) and compares it against the **sealed** `C_i`: **23 per-hop equalities over 12 (scenario, arm) cells all hold**, and the three strong arms realize the **identical** `C_0 → C_1` — no strong baseline differs in authority granularity. All 3 × 3 F1 cells block with the cause attributable per arm (`b2_token_scope` / `b3_containment`; chain-tamper `b2_exchange_refused` via the AS's `widening-rar`), while every strong arm admits the benign call. **The row's `AT_i` is read as the per-hop authority-bearing object** — `AT_i` for the exchange arm, `P_i` for the capability arms — because §E.2 applies the same sentence to arms that mint no per-hop token, and the criterion is about granularity rather than format; offline narrowing is the *measured difference*, not normalized away. **One cell has no object and says so:** on chain-tamper `B2` was refused with **no `AT_1` issued**, which is stronger than the equality it cannot be asked. **Every equality was shown able to fail** — seven constructed worlds, each judged by the gate's *own* predicate rather than described (hop at `C_{i−1}`; RAR one element too wide; attenuation to `C_0`; unverifiable token **raises** rather than returning `∅`; a granularity-mismatched arm; a `C_0`-only arm that **admits** F1-terminal; the import scan). **D21 adjudicated** (not re-asserted) on structure + non-reuse of `boundary.py` + agreement. **Residuals:** five arms receive per-hop `C_i` and three exist, so the two unbuilt limbs are **open** — DPoP and the jti cache add binding and duplicate detection, not authority, so those limbs are *expected* to be formal, **expected not verified**; re-triggered by any `Ω`/`Γ` amendment, any AS-profile change (ADR 0017), or any change to `U_task`. **`IA-3` untouched, stays `[UNVERIFIED-IA]` for G-3 — matched authority, not cost** | [g13/REPORT.md](g13/REPORT.md) | [0017](../adr/0017-as-profile-phase2.md), [0021](../adr/0021-phase1-provisioning-in-as-process.md), [0024](../adr/0024-delegating-client-base-token-carries-the-task-grant.md) |
| G-14 | G-12/G-13 | not started | — | — |
| G-15 | claim-dependent | not started | — | — |
