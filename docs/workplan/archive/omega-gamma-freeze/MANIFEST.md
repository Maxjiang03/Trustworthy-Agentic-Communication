# `Ω`/`Γ` freeze task-specification archive — MANIFEST

> **Retrospective records — NOT pre-registration evidence.** This task specification was
> authored during the work it describes. It documents what was asked and when, for provenance
> and audit. It is **not** a pre-registered commitment, it was **not** sealed before the work,
> and it must **never** be cited as pre-registration evidence. The pre-registration is authored
> and sealed only per Part H, after the smoke gates pass.

Archived byte-for-byte (verified with `cmp` against the executed working copy at repo root,
which is removed in the same commit; no typo or formatting fixes applied), 2026-07-29:

| File | SHA-256 |
|---|---|
| `OMEGA_GAMMA_FREEZE_TASK.md` | `3ba861a1dfdde7ef37e080f791357abfe02fe2ffc6e84f39bf46785fef673b4f` |

Chronology (2026-07-28 into 2026-07-29): `docs/frozen_parameters.md` **row 8** was set — the
single unset item that blocked gate G-2 outright, forced the C1 stand-in in `smoke/g4/DESIGN.md`
§9, and gated `B-cap` and `B3`. The pass recorded first that the architecture document specifies
`Ω`'s type, role and constraints but **nowhere enumerates its members**, so the contents are a
decision rather than a derivation; then froze a **seven-element** `(action, resource)` ontology
over five MCP tools with a stated necessity and a document citation for every element, a
US-ASCII/lowercase string encoding (load-bearing because RAR containment compares byte-exactly
with no normalization), and `Γ` — the MSc-profile authorizer written against G-2's four criteria,
shipped with a **matched `−attenuation` ablation stored as a delta** that cannot differ in more
than the one respect it is named for. `H(Γ)` reuses the ADR 0003/0009 tagged/versioned/
length-delimited family under a third domain tag, over the whole document so that widening `Ω`
(which widens every `C_i`) is detected too. **Full scope was retained — not a triage:** all nine
arms, every Part G gate and the §K demonstration stayed in scope, and rows 4, 5 and 6 (the
context-label, task-authorization and allowed-sink **policies**) were deliberately left UNSET, as
were rows 1–3, 7 and 9. **No gate was run or adjudicated**: G-2 remains *not run* and IA-2 remains
**[UNVERIFIED-IA]**. Recorded in **ADR 0016**. Verified on Windows: `pre-commit` clean,
`63 passed` (the pre-existing 42 plus 21 new structural tests for the frozen artifact).
