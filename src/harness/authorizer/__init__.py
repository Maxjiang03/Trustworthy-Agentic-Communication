"""Frozen authorizer configuration: `Omega`, `Gamma`, and `H(Gamma)` (ADR 0016).

`docs/frozen_parameters.md` row 8. The artifact itself is
`omega_gamma_v1.json`; `frozen_config.py` loads, validates, and hashes it.
"""
