"""The SUT child process (EXP5 STEP 3): agents, arms and the arm's decision.

Spawned by `src/harness/sut_process.py` with `python -m src.sut.sut_process`,
never imported by it -- the rule ADR 0015 rule 4 set for the AS. `wire.py` is
the JSON-only codec both sides duplicate as a wire-level fact.
"""
