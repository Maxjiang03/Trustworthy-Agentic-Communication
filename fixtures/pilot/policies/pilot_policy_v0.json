{
  "_banner": "PILOT-PROVISIONAL — NOT frozen rows 4/6/10. This object stands in for the context-label policy (row 4), the allowed-sink policy (row 6), and the high-risk action set (row 10), all UNSET in docs/frozen_parameters.md. It exists so the B3 decision path's construction can fail closed when NO policy is injected, and it may never drive a confirmatory run: the loader refuses run_mode='confirmatory' outright. F4 and F5 stay unscored until the rows are frozen by their own ADRs.",
  "profile": "pilot",
  "context": {
    "labels_supported": false,
    "note": "Rows 4 and 6 are UNSET: no label vocabulary, no label->outcome mapping, no allowed-sink policy exists. The pilot corpus carries no LabelAssertion, so context_policy_ok is not load-bearing on any pilot scenario; a presented label fails closed rather than being evaluated by an invented policy."
  },
  "approval": {
    "high_risk_actions": [],
    "note": "Row 10 is UNSET: no high-risk action set exists, so this set is EMPTY — no pilot action requires approval, and a presented approval artifact fails closed because authz_context_hash has no fixed construction yet (ADR 0009 category (c); G-15)."
  }
}
